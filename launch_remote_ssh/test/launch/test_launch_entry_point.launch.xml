<launch>
<!-- TODO(ngmor) Until catch_ros2 is updated with exposing these actions to XML, this will have to just be manual -->
    <arg name="user" />
    <arg name="machine"/>

    <!-- TODO(ngmor) have to figure out how to run a command to copy over -->
    <arg name="source_path"/>

    <!-- <log message="$(command 'ros2 run launch_remote_ssh copy_install_space.py $\(user\) $\(machine\) $\(source_path\) /tmp/launch_remote_ssh_test/')"/> -->
    <!-- <log message="$(command 'ros2 run launch_remote_ssh copy_install_space.py $(user) $(machine) $(source_path) /tmp/launch_remote_ssh_test/ ')"/> -->
                <!-- ' USER MACHINE LOCAL_INSTALL_SPACE REMOTE_INSTALL_SPACE [options]' \')" -->

    <let name="param1" value="-0.567"/>
    <let name="param2" value="this is a sentence!"/>
    <let name="param3" value="453"/>

    <launch_remote_ssh
        user="$(var user)"
        machine="$(var machine)"
        package="launch_remote_ssh"
        file="test_launch_remotely_launched.launch.xml"
    >
        <source_path path="$(var source_path)"/>
        <arg name="param1" value="$(var param1)"/>
        <arg name="param2" value="$(var param2)"/>
        <arg name="param3" value="$(var param3)"/>
    </launch_remote_ssh>
</launch>